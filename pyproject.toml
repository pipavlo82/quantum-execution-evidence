[project]
name = "quantum-execution-evidence"
version = "0.1.0"
description = "Offline verifier for synthetic and preserved live stochastic execution evidence"
requires-python = ">=3.12"
dependencies = []

[tool.qev]
profile = "quantum-evidence.v0"
license-status = "pending"
